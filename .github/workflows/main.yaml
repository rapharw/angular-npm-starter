on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop

jobs:
  build:
    uses: rapharw/github-wf-templates/.github/workflows/angular-npm.yaml@v0.0.14-alpha
    secrets:
      APPLICATION_RUNTIME: angular
      BUILD_COMPILE: npm